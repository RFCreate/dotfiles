#!/bin/sh

# Get brightness percent
brightness="$(brightnessctl | grep '%' | awk '{print $4}' | tr -d %\(\))"
#Set notification icon
icon="display-brightness-symbolic"

# Send notification
notify-send "Brightness: ${brightness}%" -h int:value:"${brightness}" -i "${icon}" -r 10001 -t 2000 -u normal
